This Odoo module provides a specific importer for product data sourced from a
Canalocio CSV feed delivered via HTTP/HTTPS.

Built upon the Connector and Connector_importer frameworks,
it extends the standard Odoo product.template model by creating and
updating records based on the external data.
